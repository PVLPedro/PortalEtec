@props (['modal'])

<span
    id="close-button"
    class="keep-auto absolute top-large right-large flex items-center justify-center rounded-small bg-bg-primary p-smaller hover:bg-bg-primary-hover"
    {{ $attributes }}
>
    <x-lucide-x />
</span>
